


  void items(item) {
    switch (item) {
      case 'A':
        state.first;
        break;
      case 'B':
        state[1];
        break;
      case 'C':
        state[2];
        break;
      case 'D':
        state.last;
        break;
    }
    state = item;
    print(state);
  }
    // var name = List<String>;
    //       var name = state.copyWith();
  


반복문 뭐가 좋을까 for
Widget title(){
  return Consumer(
          builder: (context, ref, child) {
 final state = ref.watch(storeViewModelProvider1202).length;
 return state[0]
}
  );
  }

   var name = state;
